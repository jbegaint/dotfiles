#! perl -w

sub cycle {
	my ($self) = @_;

	my $output = `/home/jean/.dynamic-colors/bin/dynamic-colors cycle`;
	$self->cmd_parse($output);

	()
}

sub on_user_command {
	my ($self, $cmd) = @_;

	if ($cmd eq "urxvt-colors:cycle") {
		$self->cycle;
	}

	()
}

