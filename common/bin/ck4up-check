#!/bin/bash
# Check for ck4up updates and notify the user

# get number of new/ updated web pages
n=$(ck4up -k | grep -E 'new:|diff:' | wc -l)

if [ $n != 0 ]
then
	n_update=$([[ $n > 1 ]] && echo "$n updates" || echo "$n update")
	notify-send '[CK4UP]' "$n_update found \!"
fi

exit 0
